//! NOTE: this file is autogenerated, DO NOT MODIFY
//--------------------------------------------------------------------------------
// Section: Constants (0)
//--------------------------------------------------------------------------------

//--------------------------------------------------------------------------------
// Section: Types (30)
//--------------------------------------------------------------------------------
pub const NS_CMD_FLAGS = extern enum(i32) {
    CMD_FLAG_PRIVATE = 1,
    CMD_FLAG_INTERACTIVE = 2,
    CMD_FLAG_LOCAL = 8,
    CMD_FLAG_ONLINE = 16,
    CMD_FLAG_HIDDEN = 32,
    CMD_FLAG_LIMIT_MASK = 65535,
    CMD_FLAG_PRIORITY = -2147483648,
};
pub const CMD_FLAG_PRIVATE = NS_CMD_FLAGS.CMD_FLAG_PRIVATE;
pub const CMD_FLAG_INTERACTIVE = NS_CMD_FLAGS.CMD_FLAG_INTERACTIVE;
pub const CMD_FLAG_LOCAL = NS_CMD_FLAGS.CMD_FLAG_LOCAL;
pub const CMD_FLAG_ONLINE = NS_CMD_FLAGS.CMD_FLAG_ONLINE;
pub const CMD_FLAG_HIDDEN = NS_CMD_FLAGS.CMD_FLAG_HIDDEN;
pub const CMD_FLAG_LIMIT_MASK = NS_CMD_FLAGS.CMD_FLAG_LIMIT_MASK;
pub const CMD_FLAG_PRIORITY = NS_CMD_FLAGS.CMD_FLAG_PRIORITY;

pub const NS_REQS = extern enum(i32) {
    NS_REQ_ZERO = 0,
    NS_REQ_PRESENT = 1,
    NS_REQ_ALLOW_MULTIPLE = 2,
    NS_REQ_ONE_OR_MORE = 3,
};
pub const NS_REQ_ZERO = NS_REQS.NS_REQ_ZERO;
pub const NS_REQ_PRESENT = NS_REQS.NS_REQ_PRESENT;
pub const NS_REQ_ALLOW_MULTIPLE = NS_REQS.NS_REQ_ALLOW_MULTIPLE;
pub const NS_REQ_ONE_OR_MORE = NS_REQS.NS_REQ_ONE_OR_MORE;

pub const NS_EVENTS = extern enum(i32) {
    NS_EVENT_LOOP = 65536,
    NS_EVENT_LAST_N = 1,
    NS_EVENT_LAST_SECS = 2,
    NS_EVENT_FROM_N = 4,
    NS_EVENT_FROM_START = 8,
};
pub const NS_EVENT_LOOP = NS_EVENTS.NS_EVENT_LOOP;
pub const NS_EVENT_LAST_N = NS_EVENTS.NS_EVENT_LAST_N;
pub const NS_EVENT_LAST_SECS = NS_EVENTS.NS_EVENT_LAST_SECS;
pub const NS_EVENT_FROM_N = NS_EVENTS.NS_EVENT_FROM_N;
pub const NS_EVENT_FROM_START = NS_EVENTS.NS_EVENT_FROM_START;

pub const NS_MODE_CHANGE = extern enum(i32) {
    NETSH_COMMIT = 0,
    NETSH_UNCOMMIT = 1,
    NETSH_FLUSH = 2,
    NETSH_COMMIT_STATE = 3,
    NETSH_SAVE = 4,
};
pub const NETSH_COMMIT = NS_MODE_CHANGE.NETSH_COMMIT;
pub const NETSH_UNCOMMIT = NS_MODE_CHANGE.NETSH_UNCOMMIT;
pub const NETSH_FLUSH = NS_MODE_CHANGE.NETSH_FLUSH;
pub const NETSH_COMMIT_STATE = NS_MODE_CHANGE.NETSH_COMMIT_STATE;
pub const NETSH_SAVE = NS_MODE_CHANGE.NETSH_SAVE;

pub const TOKEN_VALUE = extern struct {
    pwszToken: [*:0]const u16,
    dwValue: u32,
};

pub const GET_RESOURCE_STRING_FN = fn(
    dwMsgID: u32,
    lpBuffer: PWSTR,
    nBufferMax: u32,
) callconv(@import("std").os.windows.WINAPI) u32;

pub const PGET_RESOURCE_STRING_FN = fn(
) callconv(@import("std").os.windows.WINAPI) u32;

pub const NS_CONTEXT_COMMIT_FN = fn(
    dwAction: u32,
) callconv(@import("std").os.windows.WINAPI) u32;

pub const PNS_CONTEXT_COMMIT_FN = fn(
) callconv(@import("std").os.windows.WINAPI) u32;

pub const NS_CONTEXT_CONNECT_FN = fn(
    pwszMachine: [*:0]const u16,
) callconv(@import("std").os.windows.WINAPI) u32;

pub const PNS_CONTEXT_CONNECT_FN = fn(
) callconv(@import("std").os.windows.WINAPI) u32;

pub const NS_CONTEXT_DUMP_FN = fn(
    pwszRouter: [*:0]const u16,
    ppwcArguments: [*]PWSTR,
    dwArgCount: u32,
    pvData: *const c_void,
) callconv(@import("std").os.windows.WINAPI) u32;

pub const PNS_CONTEXT_DUMP_FN = fn(
) callconv(@import("std").os.windows.WINAPI) u32;

pub const NS_DLL_STOP_FN = fn(
    dwReserved: u32,
) callconv(@import("std").os.windows.WINAPI) u32;

pub const PNS_DLL_STOP_FN = fn(
) callconv(@import("std").os.windows.WINAPI) u32;

pub const NS_HELPER_START_FN = fn(
    pguidParent: *const Guid,
    dwVersion: u32,
) callconv(@import("std").os.windows.WINAPI) u32;

pub const PNS_HELPER_START_FN = fn(
) callconv(@import("std").os.windows.WINAPI) u32;

pub const NS_HELPER_STOP_FN = fn(
    dwReserved: u32,
) callconv(@import("std").os.windows.WINAPI) u32;

pub const PNS_HELPER_STOP_FN = fn(
) callconv(@import("std").os.windows.WINAPI) u32;

pub const FN_HANDLE_CMD = fn(
    pwszMachine: [*:0]const u16,
    ppwcArguments: [*]PWSTR,
    dwCurrentIndex: u32,
    dwArgCount: u32,
    dwFlags: u32,
    pvData: *const c_void,
    pbDone: *BOOL,
) callconv(@import("std").os.windows.WINAPI) u32;

pub const PFN_HANDLE_CMD = fn(
) callconv(@import("std").os.windows.WINAPI) u32;

pub const NS_OSVERSIONCHECK = fn(
    CIMOSType: u32,
    CIMOSProductSuite: u32,
    CIMOSVersion: [*:0]const u16,
    CIMOSBuildNumber: [*:0]const u16,
    CIMServicePackMajorVersion: [*:0]const u16,
    CIMServicePackMinorVersion: [*:0]const u16,
    uiReserved: u32,
    dwReserved: u32,
) callconv(@import("std").os.windows.WINAPI) BOOL;

pub const PNS_OSVERSIONCHECK = fn(
) callconv(@import("std").os.windows.WINAPI) BOOL;

pub const NS_HELPER_ATTRIBUTES = extern struct {
    Anonymous: NS_HELPER_ATTRIBUTES._Anonymous_e__Union,
    guidHelper: Guid,
    pfnStart: PNS_HELPER_START_FN,
    pfnStop: PNS_HELPER_STOP_FN,
    const _Anonymous_e__Union = u32; // TODO: generate this nested type!
};

pub const CMD_ENTRY = extern struct {
    pwszCmdToken: [*:0]const u16,
    pfnCmdHandler: PFN_HANDLE_CMD,
    dwShortCmdHelpToken: u32,
    dwCmdHlpToken: u32,
    dwFlags: u32,
    pOsVersionCheck: PNS_OSVERSIONCHECK,
};

pub const CMD_GROUP_ENTRY = extern struct {
    pwszCmdGroupToken: [*:0]const u16,
    dwShortCmdHelpToken: u32,
    ulCmdGroupSize: u32,
    dwFlags: u32,
    pCmdGroup: *CMD_ENTRY,
    pOsVersionCheck: PNS_OSVERSIONCHECK,
};

pub const NS_CONTEXT_ATTRIBUTES = extern struct {
    Anonymous: NS_CONTEXT_ATTRIBUTES._Anonymous_e__Union,
    pwszContext: PWSTR,
    guidHelper: Guid,
    dwFlags: u32,
    ulPriority: u32,
    ulNumTopCmds: u32,
    pTopCmds: *CMD_ENTRY,
    ulNumGroups: u32,
    pCmdGroups: *CMD_GROUP_ENTRY,
    pfnCommitFn: PNS_CONTEXT_COMMIT_FN,
    pfnDumpFn: PNS_CONTEXT_DUMP_FN,
    pfnConnectFn: PNS_CONTEXT_CONNECT_FN,
    pReserved: *c_void,
    pfnOsVersionCheck: PNS_OSVERSIONCHECK,
    const _Anonymous_e__Union = u32; // TODO: generate this nested type!
};

pub const TAG_TYPE = extern struct {
    pwszTag: [*:0]const u16,
    dwRequired: u32,
    bPresent: BOOL,
};

pub const NS_DLL_INIT_FN = fn(
    dwNetshVersion: u32,
    pReserved: *c_void,
) callconv(@import("std").os.windows.WINAPI) u32;

pub const PNS_DLL_INIT_FN = fn(
) callconv(@import("std").os.windows.WINAPI) u32;


//--------------------------------------------------------------------------------
// Section: Functions (8)
//--------------------------------------------------------------------------------
pub extern "NETSH" fn MatchEnumTag(
    hModule: HANDLE,
    pwcArg: [*:0]const u16,
    dwNumArg: u32,
    pEnumTable: *const TOKEN_VALUE,
    pdwValue: *u32,
) callconv(@import("std").os.windows.WINAPI) u32;

pub extern "NETSH" fn MatchToken(
    pwszUserToken: [*:0]const u16,
    pwszCmdToken: [*:0]const u16,
) callconv(@import("std").os.windows.WINAPI) BOOL;

pub extern "NETSH" fn PreprocessCommand(
    hModule: HANDLE,
    ppwcArguments: [*]PWSTR,
    dwCurrentIndex: u32,
    dwArgCount: u32,
    pttTags: ?[*]TAG_TYPE,
    dwTagCount: u32,
    dwMinArgs: u32,
    dwMaxArgs: u32,
    pdwTagType: ?*u32,
) callconv(@import("std").os.windows.WINAPI) u32;

pub extern "NETSH" fn PrintError(
    hModule: HANDLE,
    dwErrId: u32,
) callconv(@import("std").os.windows.WINAPI) u32;

pub extern "NETSH" fn PrintMessageFromModule(
    hModule: HANDLE,
    dwMsgId: u32,
) callconv(@import("std").os.windows.WINAPI) u32;

pub extern "NETSH" fn PrintMessage(
    pwszFormat: [*:0]const u16,
) callconv(@import("std").os.windows.WINAPI) u32;

pub extern "NETSH" fn RegisterContext(
    pChildContext: *const NS_CONTEXT_ATTRIBUTES,
) callconv(@import("std").os.windows.WINAPI) u32;

pub extern "NETSH" fn RegisterHelper(
    pguidParentContext: *const Guid,
    pfnRegisterSubContext: *const NS_HELPER_ATTRIBUTES,
) callconv(@import("std").os.windows.WINAPI) u32;


//--------------------------------------------------------------------------------
// Section: Unicode Aliases (0)
//--------------------------------------------------------------------------------
pub usingnamespace switch (@import("../zig.zig").unicode_mode) {
    .ansi => struct {
    },
    .wide => struct {
    },
    .unspecified => if (@import("builtin").is_test) struct {
    } else struct {
    },
};
//--------------------------------------------------------------------------------
// Section: Imports (4)
//--------------------------------------------------------------------------------
const Guid = @import("../zig.zig").Guid;
const PWSTR = @import("system_services.zig").PWSTR;
const HANDLE = @import("system_services.zig").HANDLE;
const BOOL = @import("system_services.zig").BOOL;

test {
    // The following '_ = <FuncPtrType>' lines are a workaround for https://github.com/ziglang/zig/issues/4476
    _ = GET_RESOURCE_STRING_FN;
    _ = PGET_RESOURCE_STRING_FN;
    _ = NS_CONTEXT_COMMIT_FN;
    _ = PNS_CONTEXT_COMMIT_FN;
    _ = NS_CONTEXT_CONNECT_FN;
    _ = PNS_CONTEXT_CONNECT_FN;
    _ = NS_CONTEXT_DUMP_FN;
    _ = PNS_CONTEXT_DUMP_FN;
    _ = NS_DLL_STOP_FN;
    _ = PNS_DLL_STOP_FN;
    _ = NS_HELPER_START_FN;
    _ = PNS_HELPER_START_FN;
    _ = NS_HELPER_STOP_FN;
    _ = PNS_HELPER_STOP_FN;
    _ = FN_HANDLE_CMD;
    _ = PFN_HANDLE_CMD;
    _ = NS_OSVERSIONCHECK;
    _ = PNS_OSVERSIONCHECK;
    _ = NS_DLL_INIT_FN;
    _ = PNS_DLL_INIT_FN;

    const constant_export_count = 0;
    const type_export_count = 30;
    const enum_value_export_count = 21;
    const com_iface_id_export_count = 0;
    const com_class_id_export_count = 0;
    const func_export_count = 8;
    const unicode_alias_count = 0;
    const import_count = 4;
    @setEvalBranchQuota(
        constant_export_count +
        type_export_count +
        enum_value_export_count +
        com_iface_id_export_count * 2 + // * 2 for value and ptr
        com_class_id_export_count * 2 + // * 2 for value and ptr
        func_export_count +
        unicode_alias_count +
        import_count +
        2 // TODO: why do I need these extra 2?
    );
    @import("std").testing.refAllDecls(@This());
}
