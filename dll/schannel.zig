//! NOTE: this file is autogenerated, DO NOT MODIFY
//--------------------------------------------------------------------------------
// Section: Functions (8)
//--------------------------------------------------------------------------------
// TODO: this type is limited to platform 'windows5.1.2600'
pub extern "schannel" fn SslCrackCertificate(
    pbCertificate: ?*u8,
    cbCertificate: u32,
    dwFlags: u32,
    ppCertificate: ?*?*X509Certificate,
) callconv(.winapi) BOOL;

// TODO: this type is limited to platform 'windows5.1.2600'
pub extern "schannel" fn SslEmptyCacheA(
    pszTargetName: ?[*:0]u8,
    dwFlags: u32,
) callconv(.winapi) BOOL;

// TODO: this type is limited to platform 'windows5.1.2600'
pub extern "schannel" fn SslEmptyCacheW(
    pszTargetName: ?[*:0]u16,
    dwFlags: u32,
) callconv(.winapi) BOOL;

// TODO: this type is limited to platform 'windows5.1.2600'
pub extern "schannel" fn SslFreeCertificate(
    pCertificate: ?*X509Certificate,
) callconv(.winapi) void;

pub extern "schannel" fn SslGenerateRandomBits(
    pRandomData: ?*u8,
    cRandomData: i32,
) callconv(.winapi) void;

pub extern "schannel" fn SslGetExtensions(
    clientHello: [*:0]const u8,
    clientHelloByteSize: u32,
    genericExtensions: [*]SCH_EXTENSION_DATA,
    genericExtensionsCount: u8,
    bytesToRead: ?*u32,
    flags: SchGetExtensionsOptions,
) callconv(.winapi) HRESULT;

pub extern "schannel" fn SslGetMaximumKeySize(
    Reserved: u32,
) callconv(.winapi) u32;

// TODO: this type is limited to platform 'windows8.0'
pub extern "schannel" fn SslGetServerIdentity(
    /// parameter "ClientHelloSize" is the size in bytes
    ClientHello: ?*u8,
    ClientHelloSize: u32,
    ServerIdentity: ?*?*u8,
    ServerIdentitySize: ?*u32,
    Flags: u32,
) callconv(.winapi) HRESULT;


//--------------------------------------------------------------------------------
// Section: Imports (5)
//--------------------------------------------------------------------------------
const BOOL = i32;
const HRESULT = @import("../win32/zig.zig").HRESULT;
const SCH_EXTENSION_DATA = @import("../win32/security/authentication/identity.zig").SCH_EXTENSION_DATA;
const SchGetExtensionsOptions = @import("../win32/security/authentication/identity.zig").SchGetExtensionsOptions;
const X509Certificate = @import("../win32/security/authentication/identity.zig").X509Certificate;

test {
    @setEvalBranchQuota(
        comptime @import("std").meta.declarations(@This()).len * 3
    );

    // reference all the pub declarations
    if (!@import("builtin").is_test) return;
    inline for (comptime @import("std").meta.declarations(@This())) |decl| {
        _ = @field(@This(), decl.name);
    }
}
