//! NOTE: this file is autogenerated, DO NOT MODIFY
//--------------------------------------------------------------------------------
// Section: Functions (7)
//--------------------------------------------------------------------------------
pub extern "msports" fn ComDBClaimNextFreePort(
    HComDB: ?HCOMDB,
    ComNumber: ?*u32,
) callconv(.winapi) i32;

pub extern "msports" fn ComDBClaimPort(
    HComDB: ?HCOMDB,
    ComNumber: u32,
    ForceClaim: BOOL,
    Forced: ?*BOOL,
) callconv(.winapi) i32;

pub extern "msports" fn ComDBClose(
    HComDB: ?HCOMDB,
) callconv(.winapi) i32;

pub extern "msports" fn ComDBGetCurrentPortUsage(
    HComDB: ?HCOMDB,
    /// parameter "BufferSize" is the size in bytes
    Buffer: ?*u8,
    BufferSize: u32,
    ReportType: u32,
    MaxPortsReported: ?*u32,
) callconv(.winapi) i32;

pub extern "msports" fn ComDBOpen(
    PHComDB: ?*isize,
) callconv(.winapi) i32;

pub extern "msports" fn ComDBReleasePort(
    HComDB: ?HCOMDB,
    ComNumber: u32,
) callconv(.winapi) i32;

pub extern "msports" fn ComDBResizeDatabase(
    HComDB: ?HCOMDB,
    NewSize: u32,
) callconv(.winapi) i32;


//--------------------------------------------------------------------------------
// Section: Imports (2)
//--------------------------------------------------------------------------------
const BOOL = i32;
const HCOMDB = @import("../win32/devices/serial_communication.zig").HCOMDB;

test {
    @setEvalBranchQuota(
        comptime @import("std").meta.declarations(@This()).len * 3
    );

    // reference all the pub declarations
    if (!@import("builtin").is_test) return;
    inline for (comptime @import("std").meta.declarations(@This())) |decl| {
        _ = @field(@This(), decl.name);
    }
}
