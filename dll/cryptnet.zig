//! NOTE: this file is autogenerated, DO NOT MODIFY
//--------------------------------------------------------------------------------
// Section: Functions (5)
//--------------------------------------------------------------------------------
// TODO: this type is limited to platform 'windows5.1.2600'
pub extern "cryptnet" fn CryptGetObjectUrl(
    pszUrlOid: ?[*:0]const u8,
    pvPara: ?*anyopaque,
    dwFlags: CRYPT_GET_URL_FLAGS,
    /// parameter "pcbUrlArray" is the size in bytes
    pUrlArray: ?*CRYPT_URL_ARRAY,
    pcbUrlArray: ?*u32,
    /// parameter "pcbUrlInfo" is the size in bytes
    pUrlInfo: ?*CRYPT_URL_INFO,
    pcbUrlInfo: ?*u32,
    pvReserved: ?*anyopaque,
) callconv(.winapi) BOOL;

pub extern "cryptnet" fn CryptInstallCancelRetrieval(
    pfnCancel: ?PFN_CRYPT_CANCEL_RETRIEVAL,
    pvArg: ?*const anyopaque,
    dwFlags: u32,
    pvReserved: ?*anyopaque,
) callconv(.winapi) BOOL;

// TODO: this type is limited to platform 'windows5.1.2600'
pub extern "cryptnet" fn CryptRetrieveObjectByUrlA(
    pszUrl: ?[*:0]const u8,
    pszObjectOid: ?[*:0]const u8,
    dwRetrievalFlags: u32,
    dwTimeout: u32,
    ppvObject: ?*?*anyopaque,
    hAsyncRetrieve: ?HCRYPTASYNC,
    pCredentials: ?*CRYPT_CREDENTIALS,
    pvVerify: ?*anyopaque,
    pAuxInfo: ?*CRYPT_RETRIEVE_AUX_INFO,
) callconv(.winapi) BOOL;

// TODO: this type is limited to platform 'windows5.1.2600'
pub extern "cryptnet" fn CryptRetrieveObjectByUrlW(
    pszUrl: ?[*:0]const u16,
    pszObjectOid: ?[*:0]const u8,
    dwRetrievalFlags: u32,
    dwTimeout: u32,
    ppvObject: ?*?*anyopaque,
    hAsyncRetrieve: ?HCRYPTASYNC,
    pCredentials: ?*CRYPT_CREDENTIALS,
    pvVerify: ?*anyopaque,
    pAuxInfo: ?*CRYPT_RETRIEVE_AUX_INFO,
) callconv(.winapi) BOOL;

pub extern "cryptnet" fn CryptUninstallCancelRetrieval(
    dwFlags: u32,
    pvReserved: ?*anyopaque,
) callconv(.winapi) BOOL;


//--------------------------------------------------------------------------------
// Section: Imports (8)
//--------------------------------------------------------------------------------
const BOOL = i32;
const CRYPT_CREDENTIALS = @import("../win32/security/cryptography.zig").CRYPT_CREDENTIALS;
const CRYPT_GET_URL_FLAGS = @import("../win32/security/cryptography.zig").CRYPT_GET_URL_FLAGS;
const CRYPT_RETRIEVE_AUX_INFO = @import("../win32/security/cryptography.zig").CRYPT_RETRIEVE_AUX_INFO;
const CRYPT_URL_ARRAY = @import("../win32/security/cryptography.zig").CRYPT_URL_ARRAY;
const CRYPT_URL_INFO = @import("../win32/security/cryptography.zig").CRYPT_URL_INFO;
const HCRYPTASYNC = @import("../win32/security/cryptography.zig").HCRYPTASYNC;
const PFN_CRYPT_CANCEL_RETRIEVAL = @import("../win32/security/cryptography.zig").PFN_CRYPT_CANCEL_RETRIEVAL;

test {
    @setEvalBranchQuota(
        comptime @import("std").meta.declarations(@This()).len * 3
    );

    // reference all the pub declarations
    if (!@import("builtin").is_test) return;
    inline for (comptime @import("std").meta.declarations(@This())) |decl| {
        _ = @field(@This(), decl.name);
    }
}
