//! NOTE: this file is autogenerated, DO NOT MODIFY
//--------------------------------------------------------------------------------
// Section: Functions (53)
//--------------------------------------------------------------------------------
// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptAddContextFunction(
    dwTable: BCRYPT_TABLE,
    pszContext: ?[*:0]const u16,
    dwInterface: BCRYPT_INTERFACE,
    pszFunction: ?[*:0]const u16,
    dwPosition: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptCloseAlgorithmProvider(
    hAlgorithm: BCRYPT_ALG_HANDLE,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptConfigureContext(
    dwTable: BCRYPT_TABLE,
    pszContext: ?[*:0]const u16,
    pConfig: ?*CRYPT_CONTEXT_CONFIG,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptConfigureContextFunction(
    dwTable: BCRYPT_TABLE,
    pszContext: ?[*:0]const u16,
    dwInterface: BCRYPT_INTERFACE,
    pszFunction: ?[*:0]const u16,
    pConfig: ?*CRYPT_CONTEXT_FUNCTION_CONFIG,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptCreateContext(
    dwTable: BCRYPT_TABLE,
    pszContext: ?[*:0]const u16,
    pConfig: ?*CRYPT_CONTEXT_CONFIG,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptCreateHash(
    hAlgorithm: BCRYPT_ALG_HANDLE,
    phHash: ?*BCRYPT_HASH_HANDLE,
    /// parameter "cbHashObject" is the size in bytes
    pbHashObject: ?*u8,
    cbHashObject: u32,
    /// parameter "cbSecret" is the size in bytes
    pbSecret: ?*u8,
    cbSecret: u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows8.1'
pub extern "bcrypt" fn BCryptCreateMultiHash(
    hAlgorithm: BCRYPT_ALG_HANDLE,
    phHash: ?*BCRYPT_HASH_HANDLE,
    nHashes: u32,
    /// parameter "cbHashObject" is the size in bytes
    pbHashObject: ?*u8,
    cbHashObject: u32,
    /// parameter "cbSecret" is the size in bytes
    pbSecret: ?*u8,
    cbSecret: u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptDecrypt(
    hKey: BCRYPT_KEY_HANDLE,
    /// parameter "cbInput" is the size in bytes
    pbInput: ?*u8,
    cbInput: u32,
    pPaddingInfo: ?*anyopaque,
    /// parameter "cbIV" is the size in bytes
    pbIV: ?*u8,
    cbIV: u32,
    /// parameter "cbOutput" is the size in bytes
    pbOutput: ?*u8,
    cbOutput: u32,
    pcbResult: ?*u32,
    dwFlags: NCRYPT_FLAGS,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptDeleteContext(
    dwTable: BCRYPT_TABLE,
    pszContext: ?[*:0]const u16,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptDeriveKey(
    hSharedSecret: BCRYPT_SECRET_HANDLE,
    pwszKDF: ?[*:0]const u16,
    pParameterList: ?*BCryptBufferDesc,
    /// parameter "cbDerivedKey" is the size in bytes
    pbDerivedKey: ?*u8,
    cbDerivedKey: u32,
    pcbResult: ?*u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.1'
pub extern "bcrypt" fn BCryptDeriveKeyCapi(
    hHash: BCRYPT_HASH_HANDLE,
    hTargetAlg: BCRYPT_ALG_HANDLE,
    /// parameter "cbDerivedKey" is the size in bytes
    pbDerivedKey: ?*u8,
    cbDerivedKey: u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.1'
pub extern "bcrypt" fn BCryptDeriveKeyPBKDF2(
    hPrf: BCRYPT_ALG_HANDLE,
    /// parameter "cbPassword" is the size in bytes
    pbPassword: ?*u8,
    cbPassword: u32,
    /// parameter "cbSalt" is the size in bytes
    pbSalt: ?*u8,
    cbSalt: u32,
    cIterations: u64,
    /// parameter "cbDerivedKey" is the size in bytes
    pbDerivedKey: ?*u8,
    cbDerivedKey: u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptDestroyHash(
    hHash: BCRYPT_HASH_HANDLE,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptDestroyKey(
    hKey: BCRYPT_KEY_HANDLE,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptDestroySecret(
    hSecret: BCRYPT_SECRET_HANDLE,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptDuplicateHash(
    hHash: BCRYPT_HASH_HANDLE,
    phNewHash: ?*BCRYPT_HASH_HANDLE,
    /// parameter "cbHashObject" is the size in bytes
    pbHashObject: ?*u8,
    cbHashObject: u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptDuplicateKey(
    hKey: BCRYPT_KEY_HANDLE,
    phNewKey: ?*BCRYPT_KEY_HANDLE,
    /// parameter "cbKeyObject" is the size in bytes
    pbKeyObject: ?*u8,
    cbKeyObject: u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptEncrypt(
    hKey: BCRYPT_KEY_HANDLE,
    /// parameter "cbInput" is the size in bytes
    pbInput: ?*u8,
    cbInput: u32,
    pPaddingInfo: ?*anyopaque,
    /// parameter "cbIV" is the size in bytes
    pbIV: ?*u8,
    cbIV: u32,
    /// parameter "cbOutput" is the size in bytes
    pbOutput: ?*u8,
    cbOutput: u32,
    pcbResult: ?*u32,
    dwFlags: NCRYPT_FLAGS,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptEnumAlgorithms(
    dwAlgOperations: BCRYPT_OPERATION,
    pAlgCount: ?*u32,
    ppAlgList: ?*?*BCRYPT_ALGORITHM_IDENTIFIER,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptEnumContextFunctionProviders(
    dwTable: BCRYPT_TABLE,
    pszContext: ?[*:0]const u16,
    dwInterface: BCRYPT_INTERFACE,
    pszFunction: ?[*:0]const u16,
    pcbBuffer: ?*u32,
    /// parameter "pcbBuffer" is the size in bytes
    ppBuffer: ?*?*CRYPT_CONTEXT_FUNCTION_PROVIDERS,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptEnumContextFunctions(
    dwTable: BCRYPT_TABLE,
    pszContext: ?[*:0]const u16,
    dwInterface: BCRYPT_INTERFACE,
    pcbBuffer: ?*u32,
    /// parameter "pcbBuffer" is the size in bytes
    ppBuffer: ?*?*CRYPT_CONTEXT_FUNCTIONS,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptEnumContexts(
    dwTable: BCRYPT_TABLE,
    pcbBuffer: ?*u32,
    /// parameter "pcbBuffer" is the size in bytes
    ppBuffer: ?*?*CRYPT_CONTEXTS,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptEnumProviders(
    pszAlgId: ?[*:0]const u16,
    pImplCount: ?*u32,
    ppImplList: ?*?*BCRYPT_PROVIDER_NAME,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptEnumRegisteredProviders(
    pcbBuffer: ?*u32,
    /// parameter "pcbBuffer" is the size in bytes
    ppBuffer: ?*?*CRYPT_PROVIDERS,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptExportKey(
    hKey: BCRYPT_KEY_HANDLE,
    hExportKey: BCRYPT_KEY_HANDLE,
    pszBlobType: ?[*:0]const u16,
    /// parameter "cbOutput" is the size in bytes
    pbOutput: ?*u8,
    cbOutput: u32,
    pcbResult: ?*u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptFinalizeKeyPair(
    hKey: BCRYPT_KEY_HANDLE,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptFinishHash(
    hHash: BCRYPT_HASH_HANDLE,
    /// parameter "cbOutput" is the size in bytes
    pbOutput: ?*u8,
    cbOutput: u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptFreeBuffer(
    pvBuffer: ?*anyopaque,
) callconv(.winapi) void;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptGenerateKeyPair(
    hAlgorithm: BCRYPT_ALG_HANDLE,
    phKey: ?*BCRYPT_KEY_HANDLE,
    dwLength: u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptGenerateSymmetricKey(
    hAlgorithm: BCRYPT_ALG_HANDLE,
    phKey: ?*BCRYPT_KEY_HANDLE,
    /// parameter "cbKeyObject" is the size in bytes
    pbKeyObject: ?*u8,
    cbKeyObject: u32,
    /// parameter "cbSecret" is the size in bytes
    pbSecret: ?*u8,
    cbSecret: u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptGenRandom(
    hAlgorithm: BCRYPT_ALG_HANDLE,
    /// parameter "cbBuffer" is the size in bytes
    pbBuffer: ?*u8,
    cbBuffer: u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptGetFipsAlgorithmMode(
    pfEnabled: ?*u8,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptGetProperty(
    hObject: BCRYPT_HANDLE,
    pszProperty: ?[*:0]const u16,
    /// parameter "cbOutput" is the size in bytes
    pbOutput: ?*u8,
    cbOutput: u32,
    pcbResult: ?*u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows10.0.10240'
pub extern "bcrypt" fn BCryptHash(
    hAlgorithm: BCRYPT_ALG_HANDLE,
    /// parameter "cbSecret" is the size in bytes
    pbSecret: ?*u8,
    cbSecret: u32,
    /// parameter "cbInput" is the size in bytes
    pbInput: ?*u8,
    cbInput: u32,
    /// parameter "cbOutput" is the size in bytes
    pbOutput: ?*u8,
    cbOutput: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptHashData(
    hHash: BCRYPT_HASH_HANDLE,
    /// parameter "cbInput" is the size in bytes
    pbInput: ?*u8,
    cbInput: u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptImportKey(
    hAlgorithm: BCRYPT_ALG_HANDLE,
    hImportKey: BCRYPT_KEY_HANDLE,
    pszBlobType: ?[*:0]const u16,
    phKey: ?*BCRYPT_KEY_HANDLE,
    /// parameter "cbKeyObject" is the size in bytes
    pbKeyObject: ?*u8,
    cbKeyObject: u32,
    /// parameter "cbInput" is the size in bytes
    pbInput: ?*u8,
    cbInput: u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptImportKeyPair(
    hAlgorithm: BCRYPT_ALG_HANDLE,
    hImportKey: BCRYPT_KEY_HANDLE,
    pszBlobType: ?[*:0]const u16,
    phKey: ?*BCRYPT_KEY_HANDLE,
    /// parameter "cbInput" is the size in bytes
    pbInput: ?*u8,
    cbInput: u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows8.0'
pub extern "bcrypt" fn BCryptKeyDerivation(
    hKey: BCRYPT_KEY_HANDLE,
    pParameterList: ?*BCryptBufferDesc,
    /// parameter "cbDerivedKey" is the size in bytes
    pbDerivedKey: ?*u8,
    cbDerivedKey: u32,
    pcbResult: ?*u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptOpenAlgorithmProvider(
    phAlgorithm: ?*BCRYPT_ALG_HANDLE,
    pszAlgId: ?[*:0]const u16,
    pszImplementation: ?[*:0]const u16,
    dwFlags: BCRYPT_OPEN_ALGORITHM_PROVIDER_FLAGS,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows8.1'
pub extern "bcrypt" fn BCryptProcessMultiOperations(
    hObject: BCRYPT_HANDLE,
    operationType: BCRYPT_MULTI_OPERATION_TYPE,
    /// parameter "cbOperations" is the size in bytes
    pOperations: ?*anyopaque,
    cbOperations: u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptQueryContextConfiguration(
    dwTable: BCRYPT_TABLE,
    pszContext: ?[*:0]const u16,
    pcbBuffer: ?*u32,
    /// parameter "pcbBuffer" is the size in bytes
    ppBuffer: ?*?*CRYPT_CONTEXT_CONFIG,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptQueryContextFunctionConfiguration(
    dwTable: BCRYPT_TABLE,
    pszContext: ?[*:0]const u16,
    dwInterface: BCRYPT_INTERFACE,
    pszFunction: ?[*:0]const u16,
    pcbBuffer: ?*u32,
    /// parameter "pcbBuffer" is the size in bytes
    ppBuffer: ?*?*CRYPT_CONTEXT_FUNCTION_CONFIG,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptQueryContextFunctionProperty(
    dwTable: BCRYPT_TABLE,
    pszContext: ?[*:0]const u16,
    dwInterface: BCRYPT_INTERFACE,
    pszFunction: ?[*:0]const u16,
    pszProperty: ?[*:0]const u16,
    pcbValue: ?*u32,
    /// parameter "pcbValue" is the size in bytes
    ppbValue: ?*?*u8,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptQueryProviderRegistration(
    pszProvider: ?[*:0]const u16,
    dwMode: BCRYPT_QUERY_PROVIDER_MODE,
    dwInterface: BCRYPT_INTERFACE,
    pcbBuffer: ?*u32,
    /// parameter "pcbBuffer" is the size in bytes
    ppBuffer: ?*?*CRYPT_PROVIDER_REG,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptRegisterConfigChangeNotify(
    phEvent: ?*?HANDLE,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptRemoveContextFunction(
    dwTable: BCRYPT_TABLE,
    pszContext: ?[*:0]const u16,
    dwInterface: BCRYPT_INTERFACE,
    pszFunction: ?[*:0]const u16,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptResolveProviders(
    pszContext: ?[*:0]const u16,
    dwInterface: u32,
    pszFunction: ?[*:0]const u16,
    pszProvider: ?[*:0]const u16,
    dwMode: BCRYPT_QUERY_PROVIDER_MODE,
    dwFlags: BCRYPT_RESOLVE_PROVIDERS_FLAGS,
    pcbBuffer: ?*u32,
    /// parameter "pcbBuffer" is the size in bytes
    ppBuffer: ?*?*CRYPT_PROVIDER_REFS,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptSecretAgreement(
    hPrivKey: BCRYPT_KEY_HANDLE,
    hPubKey: BCRYPT_KEY_HANDLE,
    phAgreedSecret: ?*BCRYPT_SECRET_HANDLE,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptSetContextFunctionProperty(
    dwTable: BCRYPT_TABLE,
    pszContext: ?[*:0]const u16,
    dwInterface: BCRYPT_INTERFACE,
    pszFunction: ?[*:0]const u16,
    pszProperty: ?[*:0]const u16,
    cbValue: u32,
    /// parameter "cbValue" is the size in bytes
    pbValue: ?*u8,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptSetProperty(
    hObject: BCRYPT_HANDLE,
    pszProperty: ?[*:0]const u16,
    /// parameter "cbInput" is the size in bytes
    pbInput: ?*u8,
    cbInput: u32,
    dwFlags: u32,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptSignHash(
    hKey: BCRYPT_KEY_HANDLE,
    pPaddingInfo: ?*anyopaque,
    /// parameter "cbInput" is the size in bytes
    pbInput: ?*u8,
    cbInput: u32,
    /// parameter "cbOutput" is the size in bytes
    pbOutput: ?*u8,
    cbOutput: u32,
    pcbResult: ?*u32,
    dwFlags: NCRYPT_FLAGS,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptUnregisterConfigChangeNotify(
    hEvent: ?HANDLE,
) callconv(.winapi) NTSTATUS;

// TODO: this type is limited to platform 'windows6.0.6000'
pub extern "bcrypt" fn BCryptVerifySignature(
    hKey: BCRYPT_KEY_HANDLE,
    pPaddingInfo: ?*anyopaque,
    /// parameter "cbHash" is the size in bytes
    pbHash: ?*u8,
    cbHash: u32,
    /// parameter "cbSignature" is the size in bytes
    pbSignature: ?*u8,
    cbSignature: u32,
    dwFlags: NCRYPT_FLAGS,
) callconv(.winapi) NTSTATUS;


//--------------------------------------------------------------------------------
// Section: Imports (26)
//--------------------------------------------------------------------------------
const BCRYPT_ALG_HANDLE = @import("../win32/security/cryptography.zig").BCRYPT_ALG_HANDLE;
const BCRYPT_ALGORITHM_IDENTIFIER = @import("../win32/security/cryptography.zig").BCRYPT_ALGORITHM_IDENTIFIER;
const BCRYPT_HANDLE = @import("../win32/security/cryptography.zig").BCRYPT_HANDLE;
const BCRYPT_HASH_HANDLE = @import("../win32/security/cryptography.zig").BCRYPT_HASH_HANDLE;
const BCRYPT_INTERFACE = @import("../win32/security/cryptography.zig").BCRYPT_INTERFACE;
const BCRYPT_KEY_HANDLE = @import("../win32/security/cryptography.zig").BCRYPT_KEY_HANDLE;
const BCRYPT_MULTI_OPERATION_TYPE = @import("../win32/security/cryptography.zig").BCRYPT_MULTI_OPERATION_TYPE;
const BCRYPT_OPEN_ALGORITHM_PROVIDER_FLAGS = @import("../win32/security/cryptography.zig").BCRYPT_OPEN_ALGORITHM_PROVIDER_FLAGS;
const BCRYPT_OPERATION = @import("../win32/security/cryptography.zig").BCRYPT_OPERATION;
const BCRYPT_PROVIDER_NAME = @import("../win32/security/cryptography.zig").BCRYPT_PROVIDER_NAME;
const BCRYPT_QUERY_PROVIDER_MODE = @import("../win32/security/cryptography.zig").BCRYPT_QUERY_PROVIDER_MODE;
const BCRYPT_RESOLVE_PROVIDERS_FLAGS = @import("../win32/security/cryptography.zig").BCRYPT_RESOLVE_PROVIDERS_FLAGS;
const BCRYPT_SECRET_HANDLE = @import("../win32/security/cryptography.zig").BCRYPT_SECRET_HANDLE;
const BCRYPT_TABLE = @import("../win32/security/cryptography.zig").BCRYPT_TABLE;
const BCryptBufferDesc = @import("../win32/security/cryptography.zig").BCryptBufferDesc;
const CRYPT_CONTEXT_CONFIG = @import("../win32/security/cryptography.zig").CRYPT_CONTEXT_CONFIG;
const CRYPT_CONTEXT_FUNCTION_CONFIG = @import("../win32/security/cryptography.zig").CRYPT_CONTEXT_FUNCTION_CONFIG;
const CRYPT_CONTEXT_FUNCTION_PROVIDERS = @import("../win32/security/cryptography.zig").CRYPT_CONTEXT_FUNCTION_PROVIDERS;
const CRYPT_CONTEXT_FUNCTIONS = @import("../win32/security/cryptography.zig").CRYPT_CONTEXT_FUNCTIONS;
const CRYPT_CONTEXTS = @import("../win32/security/cryptography.zig").CRYPT_CONTEXTS;
const CRYPT_PROVIDER_REFS = @import("../win32/security/cryptography.zig").CRYPT_PROVIDER_REFS;
const CRYPT_PROVIDER_REG = @import("../win32/security/cryptography.zig").CRYPT_PROVIDER_REG;
const CRYPT_PROVIDERS = @import("../win32/security/cryptography.zig").CRYPT_PROVIDERS;
const HANDLE = @import("../win32/foundation.zig").HANDLE;
const NCRYPT_FLAGS = @import("../win32/security/cryptography.zig").NCRYPT_FLAGS;
const NTSTATUS = @import("std").os.windows.NTSTATUS;

test {
    @setEvalBranchQuota(
        comptime @import("std").meta.declarations(@This()).len * 3
    );

    // reference all the pub declarations
    if (!@import("builtin").is_test) return;
    inline for (comptime @import("std").meta.declarations(@This())) |decl| {
        _ = @field(@This(), decl.name);
    }
}
